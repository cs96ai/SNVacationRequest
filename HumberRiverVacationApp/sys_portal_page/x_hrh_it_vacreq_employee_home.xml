<?xml version="1.0" encoding="UTF-8"?>
<record_update table="sp_page">
  <sp_page action="INSERT_OR_UPDATE">
    <name>x_hrh_it_vacreq_employee_home</name>
    <title>My Vacation Requests</title>
    <id>employee_vacation_home</id>
    <roles>x_hrh_it_vacreq_employee</roles>
    <public>false</public>
    <scope>x_hrh_it_vacreq</scope>
    <css><![CDATA[
/* Tropical Theme Styles */
.vacation-portal {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
  padding: 20px;
}

.tropical-header {
  background: linear-gradient(to right, #f093fb 0%, #f5576c 100%);
  border-radius: 20px;
  padding: 40px;
  margin-bottom: 30px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.2);
  color: white;
  position: relative;
  overflow: hidden;
}

.tropical-header::before {
  content: '🌴';
  font-size: 100px;
  position: absolute;
  right: 20px;
  top: 10px;
  opacity: 0.3;
}

.tropical-header h1 {
  font-size: 42px;
  font-weight: 700;
  margin: 0 0 10px 0;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
}

.balance-card {
  background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
  border-radius: 15px;
  padding: 30px;
  margin-bottom: 25px;
  box-shadow: 0 8px 30px rgba(0,0,0,0.12);
  transition: transform 0.3s ease;
}

.balance-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 40px rgba(0,0,0,0.18);
}

.balance-display {
  display: flex;
  justify-content: space-around;
  margin-top: 20px;
}

.balance-item {
  text-align: center;
  padding: 15px;
  background: rgba(255,255,255,0.5);
  border-radius: 10px;
  min-width: 150px;
}

.balance-number {
  font-size: 48px;
  font-weight: 800;
  background: linear-gradient(45deg, #667eea, #764ba2);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.balance-label {
  font-size: 14px;
  color: #666;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.vacation-card {
  background: white;
  border-radius: 15px;
  padding: 25px;
  margin-bottom: 20px;
  box-shadow: 0 5px 20px rgba(0,0,0,0.1);
  transition: all 0.3s ease;
}

.vacation-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 30px rgba(0,0,0,0.15);
}

.btn-tropical {
  background: linear-gradient(to right, #f093fb 0%, #f5576c 100%);
  border: none;
  border-radius: 25px;
  padding: 15px 40px;
  color: white;
  font-weight: 600;
  font-size: 16px;
  cursor: pointer;
  box-shadow: 0 5px 15px rgba(245, 87, 108, 0.4);
  transition: all 0.3s ease;
}

.btn-tropical:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(245, 87, 108, 0.6);
  color: white;
}

.status-badge {
  padding: 8px 16px;
  border-radius: 20px;
  font-weight: 600;
  font-size: 12px;
  text-transform: uppercase;
}

.status-pending {
  background: #fff4e6;
  color: #ff9800;
}

.status-approved {
  background: #e8f5e9;
  color: #4caf50;
}

.status-declined {
  background: #ffebee;
  color: #f44336;
}

.requests-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.palm-decoration {
  font-size: 24px;
  margin-right: 10px;
}
    ]]></css>
  </sp_page>
</record_update>

