# Humber River IT Vacation Requests

Local scaffolding of the ServiceNow scoped application **Humber River IT Vacation Requests** (`x_hrh_it_vacreq`).

This structure mimics a typical ServiceNow source control repository, with XML/JS artifacts for application, roles, tables, forms, scripts, workflows, email templates, ACLs, reports, and UI pages.

Import this repo into ServiceNow Studio via Source Control to complete configuration and deployment.
